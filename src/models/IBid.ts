export interface IBid {
  _id: string;
  amount: number;
  netAmount: number;
  account: string;
  avatar: string;
  name: string;
  createdAt: string;
  tokenSymbol: string;
  tokenAddress: string;
  customProfile: string;
}
